<Page x:Class="RSFJ.View.RojmelPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:RSFJ.View"
      xmlns:vm="clr-namespace:RSFJ.ViewModels"
      xmlns:services="clr-namespace:RSFJ.Services"
      xmlns:system="clr-namespace:System;assembly=mscorlib"
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
      Title="RojmelPage">

    <Page.DataContext>
        <vm:RojmelPageViewModel x:Name="ViewModel" />
    </Page.DataContext>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="300" />
        </Grid.ColumnDefinitions>

        <DataGrid HorizontalAlignment="Stretch" VerticalAlignment="Top" Background="WhiteSmoke"
                  ItemsSource="{Binding Path=Entries}" AutoGenerateColumns="False" CanUserAddRows="True" 
                  BorderBrush="Gray" BorderThickness="0 0 0 2" CanUserDeleteRows="True"
                  RowEditEnding="DataGrid_RowEditEnding"
                  d:DataContext="{Binding Source={d:DesignInstance Type=vm:RojmelEntryViewModel, CreateList=False}}">
            <DataGrid.Resources>
                <DataTemplate x:Key="HeadingParam1">
                    <TextBlock Text="{Binding Path=SelectedItem.HeadingParam1, FallbackValue=-, RelativeSource={RelativeSource AncestorType=DataGrid}}"/>
                </DataTemplate>
                <DataTemplate x:Key="HeadingParam2">
                    <TextBlock Text="{Binding Path=SelectedItem.HeadingParam2, FallbackValue=-, RelativeSource={RelativeSource AncestorType=DataGrid}}"/>
                </DataTemplate>
                <DataTemplate x:Key="HeadingResult">
                    <TextBlock Text="{Binding Path=SelectedItem.HeadingResult, FallbackValue=-, RelativeSource={RelativeSource AncestorType=DataGrid}}"/>
                </DataTemplate>
            </DataGrid.Resources>
            <DataGrid.CellStyle>
                <Style TargetType="DataGridCell">
                    <Setter Property="BorderBrush" Value="LightGray" />
                    <Setter Property="BorderThickness" Value="1 1 0 0" />
                </Style>
            </DataGrid.CellStyle>
            <DataGrid.Columns>
                <DataGridTextColumn Header="T-ID" IsReadOnly="True" Binding="{Binding Path=Id}" Width="Auto" />
                <DataGridTemplateColumn Header="Date" Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <DatePicker SelectedDate="{Binding Path=Date, UpdateSourceTrigger=LostFocus}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridComboBoxColumn Header="Account" SelectedItemBinding="{Binding Path=Account, UpdateSourceTrigger=PropertyChanged}" ItemsSource="{Binding Source={x:Static vm:RojmelEntryViewModel.Accounts}}" Width="2*" />
                <DataGridComboBoxColumn Header="Type" SelectedItemBinding="{Binding Path=Type, UpdateSourceTrigger=PropertyChanged}" ItemsSource="{Binding Path=DataContext.RojmelEntryTypes, Source={x:Static services:DataContextService.Instance}}" Width="2*" />

                <DataGridTemplateColumn Header="Item" Width="2*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding StockItem}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                    <DataGridTemplateColumn.CellEditingTemplate>
                        <DataTemplate>
                            <ComboBox SelectedItem="{Binding StockItem, UpdateSourceTrigger=PropertyChanged}" ItemsSource="{Binding StockItems}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellEditingTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn HeaderTemplate="{StaticResource HeadingParam1}" Binding="{Binding Path=LParam1, TargetNullValue={x:Static system:String.Empty}, UpdateSourceTrigger=LostFocus}" Width="*" />
                <DataGridTextColumn HeaderTemplate="{StaticResource HeadingParam2}" Binding="{Binding Path=LParam2, TargetNullValue={x:Static system:String.Empty}, UpdateSourceTrigger=LostFocus}" Width="*" />
                <DataGridTextColumn HeaderTemplate="{StaticResource HeadingResult}" Binding="{Binding Path=LResult, TargetNullValue={x:Static system:String.Empty}, UpdateSourceTrigger=LostFocus}" Width="*" IsReadOnly="True" />
                <DataGridTextColumn HeaderTemplate="{StaticResource HeadingParam1}" Binding="{Binding Path=RParam1, TargetNullValue={x:Static system:String.Empty}, UpdateSourceTrigger=LostFocus}" Width="*" />
                <DataGridTextColumn HeaderTemplate="{StaticResource HeadingParam2}" Binding="{Binding Path=RParam2, TargetNullValue={x:Static system:String.Empty}, UpdateSourceTrigger=LostFocus}" Width="*" />
                <DataGridTextColumn HeaderTemplate="{StaticResource HeadingResult}" Binding="{Binding Path=RResult, TargetNullValue={x:Static system:String.Empty}, UpdateSourceTrigger=LostFocus}" Width="*" IsReadOnly="True" />

                <DataGridTextColumn Header="In Stock" Binding="{Binding Path=AggregateInStock, TargetNullValue=..., UpdateSourceTrigger=PropertyChanged}" Width="*" IsReadOnly="True" />
            </DataGrid.Columns>
        </DataGrid>

        <Border Grid.Column="1" Background="WhiteSmoke" Width="300" BorderBrush="Gray" BorderThickness="1 0 0 0">

        </Border>
    </Grid>
</Page>
